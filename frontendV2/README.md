# frontendV2

This directory is a new frontend scaffold named `frontendV2`.

Current state:

- Empty scaffold created on 2026-04-02.

Next steps you might want:

- Copy files from `frontend/` into this folder if you want a duplicate.
- Initialize a new project (e.g., `npm init` / `pnpm init`) and install dependencies.
- Create `src/`, `public/`, `package.json`, and other files as needed.
